- if user.followed_by?(current_user)
  = link_to 'following', user_relationships_path(user), method: :delete, class: 'btn btn-info btn-block'
- else
  = link_to 'follow', user_relationships_path(user), method: :post, class: 'btn btn-outline-secondary btn-block'
    