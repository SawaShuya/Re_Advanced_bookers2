
- if user.followed_by?(current_user)
  = link_to 'following', user_relationships_path(user, page_from: page, display_user_id: display_user_id), method: :delete, remote: true, class: 'btn btn-info btn-block'
- else
  = link_to 'follow', user_relationships_path(user, page_from: page, display_user_id: display_user_id), method: :post, remote: true, class: 'btn btn-outline-secondary btn-block'
    