- if user.joined?(room)
  = link_to "joining", room_room_users_path(room), method: :delete,  class: 'btn btn-info btn-block'
- else
  = link_to "join", room_room_users_path(room), method: :post, class: 'btn btn-secondary btn-block'